# FormalSQL OUTCOME_EQ: a constructor regression, not a new optimization benefit.
# Keep the typed comparison explicit on both sides and preserve ordered column captures.
Filter<NullSafeEq(a2,a3) a0>(Input<t0>)|Filter<Not(Not(NullSafeEq(a8,a9))) a1>(Input<t1>)|Eq(t1,t0);Eq(a1,a0);a8 := a2;a9 := a3
